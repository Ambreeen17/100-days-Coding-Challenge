//Starts with a list of numbers
var oddNumbers = [1, 3, 5, 7, 9, 11];
//Doubles the number
var doubleNumbers = oddNumbers.map(function (oddNumber) { return oddNumber * 2; });
//show the new list of doubled numbers 
//console.log(doubleNumbers);
